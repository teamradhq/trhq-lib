import arrayToObject from '@/functions/object/arrayToObject';

describe('arrayToObject', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });

  it('should be a function', () => {
    expect(typeof arrayToObject)
      .toBe('function');
  });
});
